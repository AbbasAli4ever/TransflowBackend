# Test Environment Configuration

# Database (use separate test database!)
DATABASE_URL="postgresql://zaeemulhassan@localhost:5432/finance_test"
DATABASE_URL_TEST="postgresql://zaeemulhassan@localhost:5432/finance_test"
DATABASE_POOL_MIN=2
DATABASE_POOL_MAX=5

# Application
NODE_ENV=development
PORT=3001
API_PREFIX=api/v1

# JWT Authentication (use different secrets for testing)
JWT_SECRET=test-secret-key-minimum-32-characters-long-for-security
JWT_EXPIRATION=1h
JWT_REFRESH_SECRET=test-refresh-secret-key-also-32-characters-minimum
JWT_REFRESH_EXPIRATION=7d

# CORS
CORS_ORIGIN=http://localhost:3000,http://localhost:3001

# Rate Limiting (relaxed for tests)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=1000

# Logging
LOG_LEVEL=error
LOG_FORMAT=json

# Timezone
DEFAULT_TIMEZONE=Asia/Karachi
